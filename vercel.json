{
  "buildCommand": "cd frontend && cp *.html *.css *.js dist/ 2>/dev/null || true",
  "outputDirectory": "frontend",
  "installCommand": "echo 'No install needed'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
